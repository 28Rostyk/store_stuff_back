const { getCategories, addCategories } = require("./category");
const { getProducts, addProducts } = require("./products");

module.exports = {
  getCategories,
  addCategories,
  getProducts,
  addProducts,
};
